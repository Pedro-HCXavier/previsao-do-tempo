## 🌦️ Weather CLI em Go
Um aplicativo de linha de comando (CLI) desenvolvido em Go que consome a API da OpenWeatherMap para fornecer informações climáticas em tempo real. 

O projeto realiza a geolocalização de uma cidade informada e, a partir das coordenadas (latitude e longitude), retorna a temperatura atual e a descrição do clima.

O projeto foi todo feito vom a standard library do Go.